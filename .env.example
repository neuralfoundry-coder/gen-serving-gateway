# Docker Hub Credentials
# Copy this file to .env and fill in your values
# DO NOT commit .env to version control!

# Your Docker Hub username or organization name
# (supports both DOCKER_USERNAME and DOCKER_HUB_USERNAME)
DOCKER_HUB_USERNAME=your-dockerhub-username

# Docker Hub Access Token (create at https://hub.docker.com/settings/security)
# (supports both DOCKER_ACCESS_TOKEN and DOCKER_HUB_TOKEN)
DOCKER_HUB_TOKEN=your-access-token

# Image name (without username prefix) - optional, defaults to generative-img-serving
IMAGE_NAME=generative-img-serving
